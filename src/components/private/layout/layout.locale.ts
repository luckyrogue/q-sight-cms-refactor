import { mergeLocales } from "@/locale/local.util.ts";
import { DRAWER_LOCALE } from "@/components/private/layout/sider/drawer.locale.ts";

export const PRIVATE_LAYOUT_COMPONENTS_LOCALE = mergeLocales([DRAWER_LOCALE]);
